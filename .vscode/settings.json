{
  // Existing settings...

  "workbench.colorCustomizations": {
    // Set the title bar (top bar) color
    "titleBar.activeBackground": "#000bde",
    "titleBar.inactiveBackground": "#000bde",

    // Set the activity bar (side bar) color
    "activityBar.background": "#333333"
  }
}
